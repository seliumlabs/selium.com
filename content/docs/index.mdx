---
title: "Overview"
description: "Understand Selium's docs structure and the core runtime flow."
---

# Selium documentation

_**Documentation will be updated shortly. In the meantime, [checkout some examples](https://github.com/seliumlabs/selium/tree/main/examples/).**_

Selium is a modern, composable hypervisor for WebAssembly.

## Quickstart

```bash
# Download and install Selium Runtime and its system modules
curl --proto '=https' --tlsv1.2 -sSfL https://selium.com/install.sh | sh

# Start a Selium Runtime for development
selium-runtime \
    --module 'path=selium_remote_client_server.wasm;capabilities=ChannelLifecycle,ChannelReader,ChannelWriter,ProcessLifecycle,NetQuicBind,NetQuicAccept,NetQuicRead,NetQuicWrite;args=utf8:localhost,u16:7000' \
    --module 'path=selium_switchboard_server.wasm;capabilities=ChannelLifecycle,ChannelReader,ChannelWriter,SingletonRegistry' \
    --module 'path=selium_atlas_server.wasm;capabilities=ChannelLifecycle,ChannelReader,ChannelWriter,SingletonRegistry' &

# Query it to make sure it's online
selium ps
```

## Minimal service

```rust
use selium_userland::entrypoint;
use tracing::info;

#[entrypoint]
fn hello() {
    info!("Hello, world!");
}
```

## What to read next

- Get from zero to a deployed service in Getting started.
- Learn how the runtime is configured in Configuration.
