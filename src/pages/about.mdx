import { MdxLayout } from '../components/mdx-layout'

# About Selium

Selium is a high-performance, low-latency message broker designed for the cloud. It is
designed to be easy to use, with a simple API that allows you to get started quickly.

export default function MDXPage({ children }) {
  return <MdxLayout>{children}</MdxLayout>

}
